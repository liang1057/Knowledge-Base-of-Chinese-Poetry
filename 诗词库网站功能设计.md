### **概要功能设计**

#### **1. 项目结构**
- **开发目录**：`D:\WorkBuddy\Knowledge-Base-of-Chinese-Poetry\web`
- **数据库路径**：`D:\WorkBuddy\Knowledge-Base-of-Chinese-Poetry\db\kbcp.db`（已填充数据，直接拷贝到 `web/db/` 目录）
- **技术栈**：
  - **后端**：Python Flask + Flask-SQLAlchemy
  - **前端**：HTML/CSS/JS + jQuery + jstree（树控件） + Bootstrap（UI 框架）
  - **数据库**：SQLite（已有数据，无需设计）

---

### **2. 功能模块设计**

#### **2.1 诗词展示页面**
- **左侧树控件**：
  - **数据来源**：
    - **朝代**：`dynasty` 表
    - **作者**：`author` 表（关联 `dynasty_id`）
    - **诗词**：`poem` 表（关联 `author_id`）
  - **交互**：
    - 点击朝代 → 加载该朝代下的作者
    - 点击作者 → 加载该作者的诗词列表
    - 点击诗词标题 → 在中部展示诗词详情
- **中部展示区**：
  - **背景信息**：朝代、作者（不可编辑）
  - **诗词正文**：可编辑（富文本或纯文本）
  - **注释内容**：可编辑（富文本或纯文本）
- **右侧属性表**：
  - **动态属性**：从 `poem_attribute` 或类似表读取（如无，可设计为 JSON 存储）
  - **编辑模式**：所有属性可编辑，支持新增/删除属性
- **保存按钮**：
  - 中部（正文+注释）和右侧属性表 **统一保存**（AJAX 提交）

---

#### **2.2 内容管理功能**
##### **2.2.1 添加作者**
- **规则**：
  - **作者编号**：`朝代ID + （本朝最大作者编号 + 1）`
  - **示例**：若朝代 `tang` 的 ID 为 `1`，当前最大作者编号为 `10`，则新作者编号为 `111`（`1` + `10 + 1`）
- **表单字段**：
  - 朝代（下拉选择，关联 `dynasty` 表）
  - 作者姓名（文本输入）
  - 简介（可选，文本输入）
- **提交逻辑**：
  - 查询所选朝代的最大作者编号，计算新编号
  - 插入 `author` 表

##### **2.2.2 编辑作者信息**
- **表单字段**：
  - 作者姓名（可编辑）
  - 简介（可编辑）
- **限制**：
  - **作者 ID 不变**（仅更新其他字段）

##### **2.2.3 添加诗词**
- **表单字段**：
  - 作者（下拉选择，关联 `author` 表）
  - 标题（文本输入）
  - 正文（可选，初始为空）
  - 注释（可选，初始为空）
- **提交逻辑**：
  - 插入 `poem` 表，初始正文和注释为空

##### **2.2.4 删除诗词/作者**
- **诗词删除**：
  - 删除 `poem` 表记录
- **作者删除**：
  - 需先删除该作者的所有诗词（级联删除）
  - 再删除 `author` 表记录

---

### **3. 技术实现细节**
#### **3.1 后端（Flask）**
- **路由设计**：
  - `/`：诗词展示页面（GET）
  - `/api/tree`：获取树控件数据（GET）
  - `/api/poem/<id>`：获取诗词详情（GET）、保存诗词（POST）
  - `/api/author/add`：添加作者（POST）
  - `/api/author/edit/<id>`：编辑作者（POST）
  - `/api/author/delete/<id>`：删除作者（POST）
  - `/api/poem/add`：添加诗词（POST）
  - `/api/poem/delete/<id>`：删除诗词（POST）
- **数据库操作**：
  - 使用 Flask-SQLAlchemy 查询 `dynasty`、`author`、`poem` 表
  - 属性表数据可设计为 JSON 存储（如 `poem.attributes`）或单独表（如 `poem_attribute`）

#### **3.2 前端（HTML/JS）**
- **树控件（jstree）**：
  - 动态加载数据（懒加载）
  - 点击节点触发 AJAX 请求加载诗词详情
- **编辑区域**：
  - 使用 `contenteditable` 或 textarea 实现正文/注释编辑
  - 右侧属性表使用动态表单（可新增/删除行）
- **保存逻辑**：
  - 统一收集中部和右侧数据，通过 AJAX 提交到 `/api/poem/<id>`

---

### **4. 开发目录结构**
```
D:\WorkBuddy\Knowledge-Base-of-Chinese-Poetry\web\
├── app.py                # Flask 主程序
├── config.py             # 配置文件（数据库路径等）
├── templates/
│   └── index.html        # 主页面
├── static/
│   ├── css/
│   ├── js/
│   │   ├── main.js       # 前端逻辑
│   │   └── tree.js       # 树控件交互
│   └── lib/             # 第三方库（jQuery, jstree, Bootstrap）
└── db/
    └── kbcp.db          # 拷贝的数据库文件
```

---

### **5. 关键注意事项**
1. **数据库路径**：确保 `app.py` 中正确配置 `SQLALCHEMY_DATABASE_URI` 指向 `web/db/kbcp.db`。
2. **作者编号规则**：添加作者时需查询 `author` 表计算新编号。
3. **级联删除**：删除作者前需先删除其所有诗词。
4. **性能优化**：树控件采用懒加载，避免一次性加载所有数据。

---

### **6. 交付成果**
1. **完整 Flask 项目**（`web/` 目录下所有文件）
2. **使用说明文档**（README.md）：
   - 如何运行项目
   - 数据库结构说明
   - API 接口文档
