# 中华诗词知识库
Knowledge Base of Chinese Poetry (KBCP)

---

## 设计原则
先满足你现有数据改造需要

先服务知识库和 RAG

不追求一步到位到极复杂

兼顾后续扩展

### 命名原则
建议统一使用英文小写加下划线命名

类型采用 string\integer\number\boolean\array\object\null

无内容的数组类型为 [], 无文字说明为 ""

## 元数据定义

### 1、poem 诗词表

#### A基础字段
| 字段名         | 含义     |     类型 | 取值规则                                  | 是否必填 |
| ----------- | ------ | -----: | ------------------------------------- | ---- |
| `poem_id`   | 作品唯一编号 | string | 全库唯一，不可重复，建议格式 '朝代唯一编号-作者唯一编号-诗词编号'如'A-12345-678901' `poem_000001`          | 必填   |
| `title`     | 作品标题   | string | 去除首尾空格；保留规范标题；不能为空字符串                 | 必填   |
| `author`    | 作者姓名   | string | 使用标准作者名,在作者表中要有对应记录，不使用别名乱写                       | 必填   |
| `dynasty`   | 朝代唯一编号     | string | 必须能对应到 dynasty 对象                | 必填   |
注：每个朝代作者应该不会超过99999,诗词不会超过9999999
<br>

#### B结构化内容字段
| 字段名          | 含义          |            类型 | 取值规则                 | 是否必填 |
| ------------ | ----------- | ------------: | -------------------- | ---- |
| `content`    | 作品全文        |        string | 只存正文，不混入注释、网站说明、目录等 | 必填   |
| `paragraphs` | 按段落或联切分后的文本  | array[string] | 每个元素是一个自然段；顺序不可乱，比如词的上下阙  | 建议必填 |
| `sentences`  | 按句切分后的最小句单元 | array[string] | 去掉句末标点或保留都可以，但全库要统一  | 建议必填 |
| `line_count` | 行数/句数统计     |       integer | 根据切分结果生成，必须 >= 1     | 建议必填 |
| `char_count` | 正文字数统计      |       integer | 统计正文汉字数，可不含标点；全库规则统一 | 建议必填 |
<br>

#### C 文学属性字段
| 字段名              | 含义        |            类型 | 取值规则                                | 是否必填 |
| ---------------- | --------- | ------------: | ----------------------------------- | ---- |
| `genre`          | 大类体裁      |        string | 建议词表：`诗`、`词`、`曲`、`赋`、`其他`           | 建议必填 |
| `form`           | 具体形式      | string | null | 如“五绝”“七绝”“五律”“七律”“古体诗”“乐府”“词牌：水调歌头” | 建议必填 |
| `meter`          | 格式说明/格律补充 | string | null | 可写“二句一联，共四联”“双调”等                   | 可选   |
| `language_style` | 语言风格类型    | string | null | 建议：`文言`、`杂言`、`混合`                   | 可选   |
<br>

#### D 内容理解字段
| 字段名                  | 含义     |     类型 | 取值规则                         | 是否必填 |
| -------------------- | ------ | -----: | ---------------------------- | ---- |
| `description`        | 客观简介   | string | 建议 50–120 字，概括内容、主题、情感，不写太主观 | 可选 |
| `translation`        | 白话释义   | string | 用现代汉语解释原意；可为空                | 可选   |
| `appreciation`       | 赏析文本   | string | 用于教学/展示；可后续补充                | 可选   |
| `background`         | 创作背景   | string | 写作背景、情境、时代背景；不确定时可空          | 可选   |
| `historical_context` | 历史语境说明 | string | 更偏学术背景说明                     | 可选   |
<br>

#### E 知识增强与标签、检索优化字段
| 字段名               | 含义       |            类型 | 取值规则                            | 是否必填 |
| ----------------- | -------- | ------------: | ------------------------------- | ---- |
| `theme`           | 主题标签     | array[string] | 必须来自 theme 词表；如 `["思乡","月夜"]`   | 可选 |
| `style`           | 风格标签     | array[string] | 必须来自 style 词表；如 `["清新","含蓄"]`   | 可选 |
| `emotion`         | 情感标签     | array[string] | 必须来自 emotion 词表；如 `["思乡","孤寂"]` | 可选 |
| `imagery`         | 意象标签     | array[string] | 建议来自 imagery 词表；如 `["月","霜"]`   | 可选 |
| `allusions`       | 典故标签/典故项 | array[string] | 可先存典故名，后续再扩展为对象                 | 可选   |
| `keywords`        | 检索关键词    | array[string] | 可由规则+模型生成，控制在 3–10 个            | 可选 |
| `season`          | 季节标签     | string | null | 如 `春`、`夏`、`秋`、`冬`               | 可选   |
| `festival`        | 节令标签     | string | null | 如 `重阳`、`清明`、`中秋`                | 可选   |
| `places_involved` | 涉及地点     | array[string] | 地名列表，尽量标准化                      | 可选   |
| `people_involved` | 涉及人物     | array[string] | 历史人物或作品中出现的人物                   | 可选   |
<br>

#### F RAG相关字段
| 字段名                | 含义       |            类型 | 取值规则                        | 是否必填 |
| ------------------ | -------- | ------------: | --------------------------- | ---- |
| `retrieval_text`   | 向量检索用文本  |        string | 由标题、作者、朝代、正文、简介、标签拼接而成      | 可选 |
| `search_text`      | 关键词检索用文本 |        string | 面向全文检索，尽量包含高价值字段            | 可选   |
| `citation_text`    | 标准引用文本   |        string | 如“《静夜思》 李白〔唐〕：举头望明月，低头思故乡。” | 可选 |
| `aliases`          | 别名/异名    | array[string] | 标题异名、别题等                    | 可选   |
| `related_poem_ids` | 相关作品 ID  | array[string] | 互文、同题、唱和、同主题等               | 可选   |
<br>

#### G 管理字段
| 字段名             | 含义    |            类型 | 取值规则                                      | 是否必填 |
| --------------- | ----- | ------------: | ----------------------------------------- | ---- |
| `created_at`    | 建立时间  | string | null | 建议 ISO 8601 格式                            | 可选   |
| `updated_at`    | 更新时间  | string | null | 建议 ISO 8601 格式                            | 可选   |
| `data_version`  | 数据版本号 | string | null | 如 `v1.0`、`2026-04`                        | 可选   |
| `review_status` | 审核状态  | string | null | 建议：`raw`、`cleaned`、`generated`、`reviewed` | 可选   |

python time模块的datetime对象，可以直接转换为ISO 8601格式的字符串，例如：
```python
from datetime import datetime
now = datetime.now()
iso_now = now.isoformat()
print(iso_now)  # 输出：2023-10-01T12:34:56.789012
datetime_obj = datetime.fromisoformat(iso_now)
print(datetime_obj)  # 输出：2023-10-01 12:34:56.789012
```
<br>

### 2、author 诗人表

#### A 基础字段

| 字段名         | 含义     |     类型 | 取值规则                    | 是否必填 |
| ----------- | ------ | -----: | ----------------------- | ---- |
| `author_id` | 诗人唯一编号 | string | 全库唯一，建议 `朝代唯一编号+诗人编号`如'A-00001' | 必填   |
| `name`      | 标准姓名   | string | 使用统一姓名，不混别号             | 必填   |
| `dynasty`   | 所属朝代   | string | 使用统一朝代词表,如 “先秦”                | 必填   |
| `dynasty_id`     | 朝代唯一编号   | string | 如 `A`                 | 必填   |
注：每个朝代的诗人数量不超过99999,因此当前采用5位数足够
<br>

#### B 诗人信息字段
| 字段名             | 含义     |             类型 | 取值规则             | 是否必填 |
| --------------- | ------ | -------------: | ---------------- | ---- |
| `courtesy_name` | 字      |  string | null | 如“太白”其实是字/号需准确区分 | 可选   |
| `art_name`      | 号      |  array[string] | null | 如“青莲居士”          | 可选   |
| `other_names`   | 别名/异名  |  array[string] | 历史别称、常见异写        | 可选   |
| `birth_year`    | 出生年    | integer | null | 无法确定可为 null      | 可选   |
| `death_year`    | 卒年     | integer | null | 无法确定可为 null      | 可选   |
| `birth_place`   | 籍贯/出生地 |  string | null | 尽量标准化地名          | 可选   |
| `bio`           | 生平简介   |         string | 建议 80–256 字，客观简洁 | 可选 |
<br>

#### C 知识画像字段
| 字段名                       | 含义         |            类型 | 取值规则               | 是否必填 |
| ------------------------- | ---------- | ------------: | ------------------ | ---- |
| `style_summary`           | 风格概括       | array[string] | 来自 style 词表        | 可选 |
| `major_themes`            | 常见主题       | array[string] | 来自 theme 词表        | 可选 |
| `common_imagery`          | 常见意象       | array[string] | 从作品统计或人工总结         | 可选   |
| `representative_works`    | 代表作品标题列表   | array[string] | 可先存标题，后续可换 poem_id | 可选 |
| `representative_poem_ids` | 代表作品 ID 列表 | array[string] | 更推荐使用这个字段          | 可选   |
| `style_note`              | 风格说明       |        string | 用自然语言概括诗风          | 可选   |
| `historical_role`         | 文学史定位      |        string | 如“盛唐浪漫主义代表诗人”      | 可选   |

#### D 统计关联字段
| 字段名               | 含义      |            类型 | 取值规则        | 是否必填 |
| ----------------- | ------- | ------------: | ----------- | ---- |
| `poem_count`      | 收录作品数   |       integer | >= 0，由系统统计  | 可选 |
| `active_period`   | 活跃时期    | string | null | 如“盛唐”“北宋中期” | 可选   |
| `related_authors` | 相关诗人 ID | array[string] | 可用于交游、风格相近等 | 可选   |
<br>

#### E 管理字段
| 字段名             | 含义   |            类型 | 取值规则           | 是否必填 |
| --------------- | ---- | ------------: | -------------- | ---- |
| `source`        | 信息来源 | string | null | 如“文学史资料”“人工整理” | 可选   |
| `created_at`    | 建立时间 | string | null | ISO 8601       | 可选   |
| `updated_at`    | 更新时间 | string | null | ISO 8601       | 可选   |
| `review_status` | 审核状态 | string | null | 与 poem 一致      | 可选   |
<br>

### 3、dynasty 朝代表
#### A 基础字段
| 字段名         | 含义     |     类型 | 取值规则                    | 是否必填 |
| ----------- | ------ | -----: | ----------------------- | ---- |
| `dynasty_id` | 朝代唯一编号 | string | 如 `A`                 | 必填   |
| `name`      | 标准朝代名   | string | 使用统一朝代词表,如 “先秦”                | 必填   |
| `start_year` | 朝代起始年   | integer | null | 无法确定可为 null      | 可选   |
| `end_year`   | 朝代结束年   | integer | null | 无法确定可为 null      | 可选   |
| `another_name` | 别名| array[string] | null | 如['夏','商', '周', '东周', '西周','春秋','战国' ] | 可选   |
<br>

### 4、vocab 显式对象表 
#### A 基础字段
| 字段名         | 含义     |     类型 | 取值规则                    | 是否必填 |
| ----------- | ------ | -----: | ----------------------- | ---- |
| `vocab_id` | 词汇唯一编号 | string | 全库唯一 | 必填   |
| `name`   | 标准词汇类目名   | string | 如 '风格'                | 必填   |
| `key`      | 标准词汇类目键值   | string | 标准词汇的类型键值,如'style'                | 必填   |
| `value`   | 内容   | string | 使用json格式                | 必填   |
<br>
建议受控词表

```
    "vocab_id": "V-00001",
    "name": "主题",
    "key": "theme",
    "value": [
      "思乡",      "送别",      "边塞",      "怀古",      "咏史",
      "山水",      "田园",      "咏物",      "闺情",      "羁旅",
      "饮酒",      "赠答",      "节序",      "宫怨",      "爱国",
      "闲适",      "隐逸",      "月夜",      "怀人",      "写景"
    ]

    "vocab_id": "V-00002",
    "name": "风格",
    "key": "style",
    "value": [
      "豪放",      "婉约",      "沉郁",      "清新",      "雄浑",
      "含蓄",      "典雅",      "质朴",      "悲壮",      "空灵",
      "绮丽",      "旷达",      "自然",      "凝练",      "诙谐"
    ]
    
    "vocab_id": "V-00003",
    "name": "意象",
    "key": "imagery", 
    "value": [
      "月",      "柳",      "花",      "雨",      "雪",
      "云",      "风",      "山",      "水",      "夜",
      "天",      "地",      "日",      "星",      "河",
      "湖",      "江",      "海",      "松",      "竹",
      "梅",      "兰",      "菊",      "鸟",      "鱼",
    ]

    "vocab_id": "V-00004",
    "name": "情感",
    "key": "emotion",
    "value":[
      "喜悦",      "悲伤",      "愤怒",      "恐惧",      "惊讶",
      "哀愁",      "忧愁",      "忧愤",      "悲愤",      "悲凉",
      “思乡”,      "离愁",      "思君",      "思妇",      "思亲",
      "旷达",      "狂放",      "乐观",      "自大",      "惆怅"
    ]   

```
<br>

### 5、Schema 表
#### A 基础字段
| 字段名         | 含义     |     类型 | 取值规则                    | 是否必填 |
| ----------- | ------ | -----: | ----------------------- | ---- |
| `schema_id` | Schema唯一编号 | string | 全库唯一 | 必填   |
| `table` | 表名   | string | 如 'poem'                | 必填   |
| `name`      | 标准字段中文名   | string | 如 '标题'                | 必填   |
| `column`      | 标准字段键值   | string | 如 'title'                | 必填   |
| `type`   | 类型   | string | 如 'string'                | 必填   |

这个主要是根据上述元数据的描述进行构建和填充，因为SQLite中没有字段中文名，表中的字段很多，没有中文名就不容易读懂。
下面的仅作参考：这个表中的数据不是随便写入的，而应该是根据上面1~4表的元数据整理出来的。
```
    "schema_id": "S-00001",
    "table": "poem",
    "name": "标题",
    "column": "title", 
    "type": "string"
    
    "schema_id": "S-00002",
    "table": "poem",
    "name": "正文",
    "column": "content",
    "type": "string"
    
    "schema_id": "S-00003",
    "table": "poem",
    "name": "朝代",
    "column": "dynasty",
    "type": "string"
    
    "schema_id": "S-00004",
    "table": "poem",
    "name": "作者",
    "column": "author",
```
